function findOdd(A) {
  // UPDATE try adding count to obj
  let count = 0;
  const u = [...new Set(A)];
  // const obj = new Set(A);
  for (let i = 0; i < A.length; i++) {
    if (A.includes(A[i])) {
    }
  }
}

console.log(findOdd([1, 1, 2]));
